apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: sre-platform
  labels:
    app: orders-api
spec:
  selector:
    app: orders-api
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
