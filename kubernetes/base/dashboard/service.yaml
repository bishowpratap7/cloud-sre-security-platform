apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: sre-platform
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
