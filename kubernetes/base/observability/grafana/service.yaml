apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: sre-platform
  labels:
    app: grafana
    app.kubernetes.io/part-of: observability
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
  type: ClusterIP
